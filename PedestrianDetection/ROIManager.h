#pragma once
#include <vector>
#include "opencv2/opencv.hpp"

class ROIManager
{
public:
	ROIManager();
	~ROIManager();

	//bool needToEvaluate(cv::Rect& bbox, double scale, cv::Mat& rgb, depthScales[s], thermalScales[s]);
};

